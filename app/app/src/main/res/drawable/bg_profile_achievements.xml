<!-- Cartão de estatística do perfil: superfície branca, cantos arredondados,
     borda subtil (contraste sobre o fundo creme). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/border_radius_large" />
    <solid android:color="@color/background_secondary" />
    <stroke android:width="1dp" android:color="@color/background_elevated" />
</shape>
